import 'package:flutter_test/flutter_test.dart';

void main() {
  // Real P2P integration tests require physical hardware with Bluetooth.
  // This file is a placeholder that allows `flutter test` to pass in CI.
  test('Gossip smoke test', () {
    expect(1 + 1, equals(2));
  });
}
